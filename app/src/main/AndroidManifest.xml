<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.manoelh.task">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.activity.TaskFormActivity"
            android:screenOrientation="sensor"/>

        <activity
            android:name=".views.activity.LoginActivity"
            android:theme="@style/AppTheme.NoActionBar.fullScreen"
            android:screenOrientation="sensor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".views.activity.RegisterUserActivity"
            android:screenOrientation="sensor"/>

        <activity
            android:name=".views.activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"
            android:screenOrientation="sensor"/>

        <service
            android:name = ".service.NotificationService"
            android:label = "@string/app_name" >
            <intent-filter>
                <action
                    android:name = "app.java.com.manoelh.task.service.NotificationService" />
                <category android:name= "android.intent.category.DEFAULT" />
            </intent-filter>
        </service>
    </application>

</manifest>